import 'package:flutter/material.dart';

class AppColorsLight {
  // Primary / Brand
  static const Color primary = Color(0xFF27987C); // App bar color
  static const Color primaryDark = Color(0xFF0B886A);

  // Secondary colors
  static const Color secondary = Color(0xFFDEB14E);
  static const Color accent = Color(0xFFDF845A);

  // Text colors
  static const Color textPrimary = Color(0xFF3A4856);

  // Button colors
  static const Color buttonPrimary = Color(0xFF748DA3);
  static const Color buttonSecondary = Color(0xFF7A8FA4);

  // Neutrals
  static const Color gray = Color(0xFF95A5B2);
  static const Color black = Color(0xFF1E1E1E);
  // Page Background
  static const Color grayLight = Color(0xFFCCDDE4);
  static const Color white = Colors.white;

  // Red (Error / Alert)
  static const Color red = Color(0xFFE32231);
}
